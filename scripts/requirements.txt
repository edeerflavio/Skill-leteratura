requests>=2.31
telethon>=1.36
pymupdf>=1.24

# OCR opcional (só para PDFs escaneados). Escolha UM:
#   pip install rapidocr-onnxruntime pillow   # sem binário de sistema (Windows-friendly)
#   pip install pytesseract pillow            # + instalar o binário tesseract-ocr (por+eng)
